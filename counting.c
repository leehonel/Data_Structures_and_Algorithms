/*
	An algorithm that reads and displays a set of 6 numbers
	
	Program Algorithm

	start
	setup variables: number, count	//used by a and i respectively
	initialize variables: number:=0, count:=0;
	read numbers
	display the numbers
	increase count by 1
	if count <6 repeat from 4
	stop

*/
#include <stdio.h>
int main()
{
	int a, i;
	for (int i = 0; i < 5; ++i)
	{
		//printf("Hello world\n");
		scanf("%d\n",& a);
		//a++;
		//printf("%d\n",a );
		/* code */
	}
}